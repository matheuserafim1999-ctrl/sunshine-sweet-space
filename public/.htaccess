# Cache longo para assets versionáveis
<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresByType text/css                  "access plus 1 year"
    ExpiresByType application/javascript    "access plus 1 year"
    ExpiresByType image/jpeg                "access plus 1 year"
    ExpiresByType image/png                 "access plus 1 year"
    ExpiresByType image/webp                "access plus 1 year"
    ExpiresByType image/svg+xml             "access plus 1 year"
    ExpiresByType font/woff2                "access plus 1 year"
    ExpiresByType font/woff                 "access plus 1 year"
    ExpiresByType text/html                 "access plus 0 seconds"
</IfModule>

<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/plain text/html text/css application/javascript application/json image/svg+xml
</IfModule>

<IfModule mod_headers.c>
    Header set X-Content-Type-Options "nosniff"
    Header set Referrer-Policy "strict-origin-when-cross-origin"
    <FilesMatch "\.(css|js|jpg|jpeg|png|webp|gif|svg|ico|woff|woff2|ttf|otf)$">
        Header set Cache-Control "public, max-age=31536000, immutable"
    </FilesMatch>
</IfModule>

DirectoryIndex index.php index.html

Options -MultiViews

<IfModule mod_rewrite.c>
    RewriteEngine On
    # Evita /site.php/ cair em fallback/cache antigo; sempre normaliza para o PHP real
    RewriteRule ^site\.php/.*$ /site.php [R=302,L,NC]
</IfModule>

<IfModule mod_headers.c>
    <FilesMatch "^(index|site)\.php$">
        Header always set Cache-Control "no-store, no-cache, must-revalidate, max-age=0"
        Header always set Pragma "no-cache"
        Header always set Expires "0"
    </FilesMatch>
</IfModule>

# Bloqueia acesso direto ao site.php (só via require do cloaker, se configurado)
# Comente as 3 linhas abaixo se quiser permitir acesso público
# <Files "site.php">
#     Require all denied
# </Files>
