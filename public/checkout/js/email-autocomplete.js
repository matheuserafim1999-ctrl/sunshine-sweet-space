// Autocomplete simple para correos electrónicos comunes
document.addEventListener('DOMContentLoaded', function() {
    const emailInput = document.querySelector('input[name="email"]');
    if (emailInput) {
        const domains = ['gmail.com', 'hotmail.com', 'outlook.com', 'icloud.com', 'yahoo.es', 'telefonica.net'];
        // Lógica de autocomplete simplificada o eliminada si no es crítica
    }
});